name: "Danger Kotlin"
on: [pull_request]

jobs:
  - build:
    runs-on: ubuntu-latest
    name: "Run Danger"
    steps:
      - uses: actions/checkout@v1
      - name: Danger
        uses: danger/kotlin@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
  - build:
    runs-on: ubuntu-latest
    name: "Run Danger Swift"
    steps:
      - uses: actions/checkout@v1
      - name: Danger
        uses: danger/swift@swift_5.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
